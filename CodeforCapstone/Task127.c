#include <stdio.h>
int main() {
    int a, b;
    scanf("%d", &a);
    scanf("%d", &b);
    if (a == b) {
        printf("The numbers are equal.\n");
    } else {
        printf("The numbers are not equal.\n");
    }
}
